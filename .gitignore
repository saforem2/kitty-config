# Scratch / backup configs
default.conf
new.conf
kitty-prev-*.conf
font-[0-9]*.conf

# Fragment not included by kitty.conf
font-symbol-map.conf

# Python type checker cache
.pyre/

# timestamped theme snapshots — superseded by the oklch versions
themes/*.conf-*.bak
